package week4

object FunctionsAsObjects {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(86); 
  println("Welcome to the Scala worksheet");$skip(28); 
  
  val f = (x:Int) => x*x;System.out.println("""f  : Int => Int = """ + $show(f ))}
  
  
}